import setuptools

setuptools.setup (
    name = "libs_zip",
    packages = ["libs"],
    version = "0.0.0",
    download_url = "",
    classifiers=[
        "Programming Language :: Python :: 3"],
    install_requires=[
        'selenium',
        'beautifulsoup4',
    ],
)
